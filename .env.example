# Absolute origin of this deployment, used for Open Graph URLs, the sitemap
# and robots.txt. On Vercel set it to the preview or production URL.
# Optional: falls back to the club's domain from data/site.json.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
